name: StockBot

on:
  schedule:
    - cron: "0 */6 * * *"

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install Libraries
        run: pip install yfinance pandas ta python-telegram-bot
      - name: Run Bot
        run: python stockbot.py
